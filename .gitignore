# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo lock files (optional - uncomment if you want to ignore)
# Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Beads (local task tracking)
.beads/*
.needle-predispatch-sha

# Build artifacts in subdirectories
**/target/
!.beads/issues.jsonl

# bead-rs store: the durable artefact is the checkpoint plus config.json.
# (`beads.db` stays ignored — it is per-machine live state.)
!.beads/checkpoint/
!.beads/checkpoint/**
!.beads/config.json
