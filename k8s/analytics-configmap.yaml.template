apiVersion: v1
kind: ConfigMap
metadata:
  name: trace-config
  namespace: trace
data:
  s3-bucket: "my-trace-bucket"
  s3-region: "us-east-1"
  s3-prefix: "trace-events"
  iceberg-catalog-uri: "http://iceberg-rest:8181"
  iceberg-warehouse: "s3://my-trace-bucket/iceberg"
