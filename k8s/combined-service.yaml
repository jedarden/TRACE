apiVersion: v1
kind: Service
metadata:
  name: trace-combined
  namespace: trace
  labels:
    app: trace-combined
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: trace-combined
