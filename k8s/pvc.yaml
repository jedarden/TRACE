apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trace-logs
  namespace: trace
  labels:
    app: trace
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-client
